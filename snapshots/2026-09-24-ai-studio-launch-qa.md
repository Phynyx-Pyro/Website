# Phynyx AI Studio lead-journey QA — 2026-09-24

This is a point-in-time record of the separate **PhynyxPro Website** AI Studio project (`1789733833642057228`) and HighLevel location `DsRnzA2tcoiwklSgjBtR`. The published AI Studio URL is `https://phynyxpro-website.vibepreview.app`. The older `get.phynyxpro.com` site is a different build. AI Studio has **no custom domain connected** as of this inspection; no production-domain cutover was made.

## Operating path

```text
Ungated 3-number check -> baseline contact capture -> 001 intake -> lead
                                |                        |
                                | unfinished             +-> 002a incomplete recovery
                                v
Submit 8-field snapshot ("not tracked" allowed) -> result + .txt download -> 001b completion routing
                                                               +-> existing opportunity to Qualified / Needs Appointment
Direct schedule contact capture -----------------------> 001b direct routing + lead opportunity
                                                          |
                                                          +-> 002b clean email recovery + assign Ember bot*
                                                          |      +-> day-7 eligible: configured Craig review task (runtime pending), then final email
                                                          +-> 002c AI voice, affirmative voice consent only
                                                          +-> 002d SMS/AI, affirmative marketing-SMS consent only
Reply -> 002e stop nurture, mark engaged, alert Craig + create review task;
production Ember reply still requires channel deployment*;
Explicit STOP tag -> 005a opt-out; outbound DND or email unsubscribe -> 005c safety;
human handover -> 005b pause bot and notify staff.
Confirmed calendar booking -> calendar assigns round-robin owner; 003a stops recovery, advances opportunity,
                              configured Ember pause (post-edit runtime pending);
native calendar owns customer confirmation and 24h/1h email reminders.
Showed -> 004a lifecycle cleanup;
no-show/cancelled -> 004d internal human review task;
004b/004c automatic tag/stage recovery remains paused until exact-current-appointment safety is proved.
```

The measurable acquisition goal is a confirmed 30-minute call with Craig or Andrew. Use stage facts (lead, snapshot complete, booked, showed, won) and reply/handoff tags before adding numerical scores. A form submit is a lead, not an appointment. No-show/cancellation are distinct outcomes, not abandoned forms.

## Configuration and observed runtime

| Component | Current configuration | Runtime evidence |
| --- | --- | --- |
| AI Studio capture | Five required identity fields; three separate optional consents default off; native `external_form_submission` | Internal baseline and direct-only submissions created CRM contacts with the correct native Form Submitted events. |
| 001 intake `36118464-ea4a-4a5b-b5ab-a2cd614c2063` | Exact Growth Snapshot Intake trigger, version/source/incomplete tags and internal FYI | Internal initial capture completed the workflow and created a New Website Lead opportunity. |
| 001b completion/direct `ba5b5d85-e78f-442e-bd5c-d91881496ca7` | Two exact AI Studio form triggers; sets version/source/booking-followup, removes incomplete recovery, and creates a duplicate-disabled New Website Lead opportunity. A completed-Snapshot condition finds that contact's latest open Website Sales opportunity and moves it forward to Qualified / Needs Appointment; backward stage movement remains off. | Direct-only intake created one New Website Lead opportunity. A fresh baseline-then-Snapshot submission skipped duplicate creation, followed the completed branch, found the existing open opportunity, and moved that sole record to Qualified / Needs Appointment. |
| 002a incomplete `7780711c-043e-40a6-8d49-92210c96fcd9` | 15m/day2/day7 finite email cadence, current AI Studio assessment link, rechecks incomplete/unbooked/engagement; workflow-level Mon–Fri 9 AM–5 PM in contact timezone | Earlier controlled consent-off contact received its first email in AgentMail. Later cadence not yet elapsed. |
| 002b clean booking `19f5a2a8-a30d-4296-beea-09a56bf21610` | 15m/day2/day7 finite email cadence, weekday business-hour window, unbooked/unpaused/unengaged guards. The eligible branch assigns `Ember - Website Sales Concierge` Active before the first email. Its day-7 eligible branch now creates a Craig human-review task before the final email. | The completed Craig snapshot enrolled at 2:43:46 AM CDT; its first email was waiting for 9 AM. The new terminal task is published and reloaded but not yet runtime-tested. Production concierge Email/SMS channels remain **Off**; 002b email delivery is unverified. An isolated QA bot did reply to an owned alias. |
| 002c/002d | Voice after 1 day only with affirmative AI Voice Marketing Consent; SMS at 15m/day2 only with affirmative Marketing SMS Consent; each rechecks booking, pause, handoff and DND. Both workflows already had a Mon–Fri, 9 AM–5 PM **workflow-level** communication window in the contact's timezone. | Craig's specifically authorized internal test contact now stores `Consented` in AI Voice Marketing Consent, Marketing SMS Consent and Appointment Non-Marketing SMS Consent. The site baseline and snapshot completion succeeded, but no consent-on call/SMS delivery has yet been observed. A prior consent-off SMS fixture correctly ended without an SMS action. |
| 002e/005a/005b/005c | Configured: replies stop pending nurture and mark engaged; the published 002e task/alert and 005b handoff actions are not yet runtime-tested. The 002e trigger requires the website-source tag without a brittle form-version check. 005a explicit STOP applies all-channel DND/pause; 005c independently catches outbound all-channel or exact Email/SMS/Call DND and email unsubscribe; 005b handoff pauses bot and notifies Craig/Andrew. | 002e fired on two internal email replies **before** the task/alert edit; those new actions still need an internal runtime test. 005a executed on an internal contact with and without the v1 form-version field; all-channel DND persisted. 005c's all-channel, Email, SMS and Call DND triggers each enrolled isolated internal fixtures. A real unsubscribe link later triggered 005c and its pause/bot-stop actions; complete workflow removal and later-message suppression remain unproven. The 005b human handoff remains untested. |
| 003a + calendar | Exact confirmed assessment calendar; 003a stops recovery and updates opportunity while the calendar applies round-robin appointment/contact ownership. The optional post-booking bot action was changed to set Ember **Inactive with no automatic reactivation** on its eligible branch. Calendar equal-distribution round robin and native contact booking/reschedule/24h/1h email; workflow customer confirmation disabled. | Two labeled `/schedule` tests produced **Confirmed** appointments (Oct 2 and Sep 25, both 10 AM CDT), each assigned Craig, with Appointment Scheduled opportunities and completed 003a runs **before** the bot-pause edit. The calendar sent exactly one immediate matching confirmation email with an `.ics` attachment to each controlled inbox. The new bot-pause setting was saved, published and reloaded; post-edit execution awaits a clean, unpaused internal booking. The second contact retained `owner:ember-ai` despite human CRM ownership; tag semantics need a focused decision. Future 24h/1h reminders remain untested. |
| 004a/b/c/d | 004a showed cleanup remains published. Unsafe 004b no-show and 004c cancellation remain Draft because both clear contact-wide `appt:booked` before verifying whether a newer appointment exists. Published 004d creates a Craig review task only for assessment-calendar no-show/cancellation on website-tagged contacts. | 004d's controlled stale cancellation and real no-show each created one internal task without changing booked tag or stage. 004b/004c had zero total/active enrollments when paused; automatic no-show/cancellation recovery is deliberately held. |

The older draft `002b` (`2bf5b127-8c2d-4200-973c-aec295397aaa`) remains unpublished and superseded. The clean replacement is published; it was added to removal actions in 002e, 003a, 005a and 005b. No destructive deletion was performed.

*HighLevel's current [channel-routing documentation](https://help.gohighlevel.com/support/solutions/articles/155000008266-how-to-manage-conversation-ai-bot-channels-and-routing) distinguishes direct contact assignment from channel deployment. The website concierge bot is Auto-Pilot on `OpenAI GPT-5.4 Mini`, but has no deployed channels. Its Email deployment dialog was inspected and cancelled unchanged. Do not turn on a broad Email/SMS channel during internal-only QA: the published intake workflows assign this bot to contacts, and direct assignment can take priority over tag routing. A bounded internal test and explicit release gate are needed before bot replies reach non-test leads. The 5.4 Mini choice is proportionate for this narrowly scripted nurture role; no higher-cost bot model was selected.*

## Website verification

- The 3-number calculator showed 80 inquiries, 28 booked, 20 showed as 35% booked, 71.4% show and 28.6% booked-to-show drop-off without gating the visitor.
- The 8-metric result showed correct five conversion rates and an illustrative +2.1 starts scenario. Completion now saves on **Show My Growth Snapshot**, with no second submit click. The controlled contact entered 001b and 002b after that action.
- A second, explicitly consented internal assessment using Craig's authorized backup details saved all eight estimated metrics (120 inquiries, 95 contacted, 42 booked, 38 confirmed, 30 showed, 20 started, $3,000 spend, $1,200 average start value). The site displayed 8/8 tracked metrics, five valid rates, $25 cost/lead, $150 cost/start, and **Snapshot Saved**. The CRM created one contact, stored all three affirmative consent fields, completed 001b, created an opportunity and moved it to **Qualified / Needs Appointment** without a booking. This is a lead/result, not a confirmed call.
- `Download My Snapshot` produced `/Users/craigcapurso/Downloads/my-growth-snapshot.txt` with matching input values and calculations. The site explicitly promises a browser-generated `.txt`, not an emailed PDF.
- Direct booking contact capture emits the correct native `Growth Call Booking Intake` event before slot selection. It is not a booked appointment. The site claims a confirmed booking only if the provider response includes an appointment ID and booked start; a 2xx without them is shown as accepted-but-unverified and verified separately in CRM/email.
- The first live booking exposed a critical false-failure screen: after one Confirm click, the site said **“Booking didn't go through”** and invited another slot, although HighLevel booked it and emailed confirmation. The source takes that branch when a 2xx response lacks top-level `appointmentId` and/or `bookedStart`; the raw response body was not captured, so its actual schema is unknown. A narrow AI Studio fix was independently typechecked, tested (62/62) and built, then **published**. A second fresh internal booking on Sep 25 at 10 AM displayed **“Your request was received”**, advised checking email, and offered no resubmit control. CRM confirmed the booking and native email arrived at 2:49:47 AM CDT. The UI now avoids a false failure and duplicate invitation; it still cannot itself assert provider confirmation without a returned appointment ID/start.
- The live `/schedule` source had a dead availability-error Retry button (it did not retrigger the slot effect) and allowed repeated clicks on Confirm while a booking request was pending. The published September 24 update adds an explicit availability reload key and an in-flight booking guard. These reduce avoidable failures and duplicate-click risk; they do **not** prove server-side booking idempotency or a completed appointment. AI Studio reported the published build `Up to date`, and the public `/schedule` URL rendered after publication.
- A post-publication HTTP route sweep returned `200 text/html` for all 16 declared public paths, including `/growth-assessment`, `/schedule`, and `/support`. This proves route availability only; it does not prove that forms submit or messages deliver.
- The three published 002b recovery emails have accurate, finite booking-oriented copy and do not claim that a call is already booked or a personalized review has happened. Each CTA currently links to HighLevel's native `link.phynyxpro.com/widget/booking/NX2pJFAx51yOcaNIdNjL`, not AI Studio's `/schedule`; that native link rendered the correct 30-minute `Phynyx Website - Assessment` calendar in a read-only check. Keep this working alternate booking route until the AI Studio booking commit is proven; then decide whether to unify links for brand and attribution. No email copy/link was changed during this audit.
- One built-in, non-contact test of `Ember - Website Sales Concierge` asked whether a completed free Growth Snapshot requires a booking. The bot identified itself as AI, said a call is optional, and offered help understanding results or booking the 30-minute diagnostic. AI Response Info attributed the answer to that bot in Auto-Pilot mode. This is prompt-level evidence only, not proof of Email/SMS routing, a CRM action, or provider delivery. The answer omitted the site's specific on-screen/browser-generated `.txt` delivery detail; it did not promise an emailed PDF.
- The bot's Appointment Booking action selects `Phynyx Website - Assessment` (`NX2pJFAx51yOcaNIdNjL`). The live calendar settings list that exact ID as **Active**, 30-minute, round-robin. Its action editor briefly rendered a red `inactive/deleted` warning while the calendar name was still loading; the warning disappeared and `Proceed` enabled after the UI resolved the calendar. This was a transient editor state, not evidence that the calendar is inactive. The action is configured to book directly rather than only send a link; `Pause bot responses after booking` and `Trigger workflow after booking` are off, while AI cancellation and rescheduling of existing appointments are on. No booking action was executed, so its success and scope are unproven. The no-change inspection was cancelled.
- All three published 002a incomplete-recovery emails link to the **new** AI Studio `/growth-assessment` route, and days 2/7 also offer the native booking calendar. No old `get.phynyxpro.com` link was found in those messages. Important UX limitation: the assessment draft is kept in browser `sessionStorage`; opening the email link from a different tab/device is not a guaranteed resume and may restart at the free check. The copy says “finish,” not that prior answers will be restored. No email copy/link was changed during this audit.
- The legacy bridge route remains fail-closed. The formerly non-submitting `/support` draft was replaced on September 24 with a published `mailto:craig@phynyxpro.com` contact path and a visible address for manual copy. The page explicitly says that opening an email app is not sending a message. This is a working browser contact link, **not** a support form or verified inbox-delivery result.
- Source archive `backups/ai-studio/2026-09-23-pre-routing-fixes.zip` preserves the pre-fix export; `backups/ai-studio/2026-09-24-published-connected.zip` preserves the earlier connected export; `backups/ai-studio/2026-09-24-published-booking-hardening.zip` preserves the preceding published export; `backups/ai-studio/2026-09-24-booking-response-hardening-draft.zip` preserves the booking-response source; `backups/ai-studio/2026-09-24-published-support-email.zip` preserves the prior support-contact publication; and `backups/ai-studio/2026-09-24-published-snapshot-journey.zip` preserves the prior published journey. The booking-safety version is archived at `backups/ai-studio/2026-09-24-published-booking-safety-and-fallback.zip`; the **current** published full source is `backups/ai-studio/2026-09-24-published-calendar-availability-fix.zip`. These are **site source backups**, not HighLevel workflow exports.
- HighLevel's `BACKUP — Website Sales Baseline — 2026-09-23` folder contains ten unpublished/draft clones of the original 001–004c workflow set, each with zero enrollments. Before the bot repair, a separate `BACKUP — 002b Booking Recovery Clean — 2026-09-24 pre-AI-reply` clone was placed in that same folder; its Draft status and 0/0 enrollments were verified on page 2. The later 001b/005a/005b were created after the baseline snapshot and are not misrepresented as part of it.

## Automated source checks

The **current** published AI Studio export passed an independent `tsc --noEmit`, client and SSR production build, and **125/125 Vitest tests in seven files**. The immediately preceding booking-safety export passed 122/122 tests, the Snapshot-journey export passed 76/76, and the booking-hardening export passed 62/62 after its `SlotFailurePanel` repair. The prior full-project ESLint check had **26 pre-existing Prettier-only formatting errors** in untouched tests and Vite config, plus shared-component warnings; that lint count was not remeasured on the current export. Offline checks did not alter the published ZIP.

## Release gates still open

1. Verify first 002b email sends in the configured business-hour window and reaches the controlled inbox. Workflow enrollment alone is not delivery. A separate QA-only bot answered two inbound emails end to end, but the production concierge's Email channel remains **not configured**. Before customer traffic, scope and test that production routing separately; existing direct bot assignments make a tag-only channel gate insufficient.
2. A real unsubscribe link now triggered 005c; verify complete workflow removal and no later messages after STOP/DND. Test the new 002e Craig task and in-app alert with an owned inbound reply, and test 005b human handoff on an internal fixture during staff hours. Check Craig's consent-on SMS/voice branches only during their business-hour windows, and verify delivery separately from workflow execution.
3. Verify the 24h/1h native reminders for the Sep 25 QA appointment, and ensure no residual acquisition messages reach either booked fixture. Run one new, unpaused internal booking during staff hours to prove the edited 003a bot-inactive action, calendar-owned human assignment, opportunity update and staff notices together. The new public booking screen no longer invites duplicate submission, but the provider's actual 2xx response schema and server-side idempotency remain unknown. Test the published 002b day-7 task and final-email path at runtime.
4. Resolve the site domain. AI Studio currently publishes only on `vibepreview.app`; no custom domain is connected. Do not assume the old `get.phynyxpro.com` URL runs this build.
5. `/support` now has an honest email contact path, but receipt at Craig's inbox was not tested. Full-project formatting lint remains open, but is not a lead-capture blocker.
6. Repair and test the stale appointment-status edge before republishing 004b/004c. An old no-show/cancelled event can clear contact-wide `appt:booked` after a replacement booking. Both unsafe flows are Draft; published 004d now creates an internal task for manual review of the exact event and any newer confirmed booking, without changing customer status or sending outreach.
7. The concierge's broad cancel/reschedule switches were turned off in the later safety pass. Before enabling its live channel, test a real internal contact with an existing appointment to ensure the bot does not offer a duplicate booking. Direct bot assignment in 002b can override channel/tag routing; merely adding a QA tag filter to an Email deployment is not a proven isolation boundary.
8. Decide whether `owner:ember-ai` denotes conversation ownership after a booked call or should be replaced with `owner:human-call`. The second booking was assigned to Craig in the CRM, but retained the AI-owner tag from intake; 003a's cleanup removes status/nurture tags, not that owner tag. No tag change was made without auditing downstream dependencies.

Earlier post-publication readback: AI Studio's Domains page still said `No custom domains connected yet.` The published preview URL was the only listed project URL. A fresh read of 004b before its later Draft hold confirmed `Clear Prior Booked Flag` preceded the 15-minute wait and the `appt:booked` exclusion. Its condition picker exposed the contact's `Last appointment at`, but no appointment-ID comparison or safe dynamic date comparison was verified. An exploratory condition edit was cancelled. Both 004b and 004c were subsequently moved to Draft, as recorded below. Do not claim the stale-event edge repaired without an appointment-pair test.

Channel-hours correction: an internal 002c enrollment showed its **wait** due at 11:34 PM CDT. That was not evidence of a late-night call: the workflow Settings page already restricts outbound actions to Mon–Fri 9 AM–5 PM in the contact's timezone; 002d has the same setting. Temporary duplicate advance windows added to the three individual waits were removed. After reload, each wait's advance window was off and both workflows retained the published workflow-level window. HighLevel documents that a wait may end outside the window, while the next outbound action waits for the window. Actual voice/SMS delivery timing remains untested.

## Release decision at this checkpoint

**Controlled QA only; not yet approved for paid traffic.** Internal runtime evidence covers capture, calculator/result, native form events, routing, first incomplete-path email, two CRM bookings and immediate confirmations, plus one direct-only `/schedule` submission creating a single open New Website Lead opportunity. Snapshot-path opportunity dedupe and day-7 task execution remain unverified. The website handles the observed accepted-but-unverified booking response without a false failure or resubmit prompt, but provider response schema and server-side idempotency remain unknown. Production concierge Email/SMS is not deployed, so production AI replies are not proven. Clean-recovery email delivery, consented SMS/voice, timed reminders, post-edit 003a bot pause, post-edit 002e task/alert, safe stale-event automation, and human handoff remain open.

A thread follow-up is scheduled for 9:15 AM CDT September 24 and 25 to verify queued recovery email/SMS and the Sep 25 appointment's timed native reminders. The two internal live bookings above were explicitly authorized by Craig and executed once each; no customer traffic or paid signup was initiated.

## 3:05 AM CDT safety pass

- Reopened the published 004b no-show workflow. Its first action still removes the contact-wide `appt:booked` tag before the 15-minute wait. The same stale-event risk applies to 004c cancellation. No unverified appointment guard was added or published. HighLevel's appointment If/Else filter describes the trigger appointment's start/end/rescheduled state, not a query for another future appointment.
- In `Ember - Website Sales Concierge`, disabled both **Allow bot to cancel the appointment** and **Allow bot to reschedule the appointment**. Reopened the action after a full reload and verified both switches off. The native calendar confirmation email already contains customer reschedule/cancellation links.
- Aligned the bot prompt: it now directs an existing-booking change request to those confirmation links or a human, and forbids a replacement booking until cancellation is verified. A non-contact AI test said it could not change the current appointment and would help book only after cancellation. This is prompt-level evidence; no customer message or appointment action was executed.
- Disabled the bot's separate **Contact Stopped Replying** Auto Follow-up scenario. It had one AI-generated message scheduled for 24 hours, duplicating 002b's bounded day-2/day-7 nurture and potentially escaping its workflow timing/eligibility checks. A full reload showed the action **Off**. The 002b finite cadence remains the single source for outbound booking recovery; the bot may reply to inbound messages only after a safely scoped channel deployment, which remains pending.
- Craig's consented, unbooked test contact passed 002b's 15-minute eligibility check at 2:58:48 AM CDT. The bot-assignment action executed at 2:58:51 AM, and the first recovery email is **waiting for the 9 AM communication window**. This is not yet email execution or delivery; it also confirms that this contact has a direct bot assignment, so tag-only channel scoping cannot be assumed safe.
- Added one delivery fact to the bot prompt: completed Snapshots appear on-screen with a browser-generated `.txt` download; it must not promise an emailed report. The text and section formatting were read back after a full reload. The bot remains on `OpenAI GPT-5.4 Mini`; a larger runtime model was not chosen for the narrow reply task.

Release status remains **controlled QA only**. The above safety fixes do not establish live AI channel delivery, future timed messages, or stale appointment-pair safety.

## 3:20 AM CDT site and conditional-safety pass

- Scoped AI Studio builder edits to `src/routes/support.tsx` only. The exported ZIP differs from the preceding published export in that one file. Removed the dead inquiry form and replaced it with a `mailto:` CTA to the same `craig@phynyxpro.com` address already listed in the site's Terms. A preview-only Copy button did not confirm a clipboard write in the controlled browser, so it was removed; the address remains selectable. No service, backend or paid signup was added.
- Independently ran strict TypeScript checking, the client and SSR production build, and the four-file test suite (**62/62 pass**) on the final export. The draft preview and the refreshed public `/support` route rendered the new content, with no public-page console errors. AI Studio's publication dialog showed **Up to date** after the update. The archived ZIP SHA-256 is `adb8e546ac59a743ad776a6437ecac1e7410e4dce17c913f42e9d6c40cd07b38`. The mailto destination/subject was read from the rendered link; an actual inbox delivery was not tested.
- [HighLevel's appointment-status trigger documentation](https://help.gohighlevel.com/support/solutions/articles/155000002619) and [appointment If/Else filters](https://help.gohighlevel.com/support/solutions/articles/155000004050) do not establish an exact-current-appointment guard. A status-triggered old event can still clear the contact-wide `appt:booked` tag in published 004b/004c after a newer booking; no speculative guard or customer message was added. This remains a pre-traffic release gate. Bot runtime remains GPT-5.4 Mini; high reasoning was reserved for audit and design rather than routine nurture replies.

## 3:40 AM CDT isolated email-reply proof and lifecycle hold

- Duplicated the production conversation bot into `QA ONLY - Ember Website Concierge - 2026-09-24` (`Tu23LJKyzusrwLtMLGXK`). Its only deployed channel is Email with the unique `#qa-ember-email-20260924` include-tag rule; SMS, chat and other channels are not configured. The production bot's channels were not changed. The QA bot uses `OpenAI GPT-5.4 Mini`; high-reasoning audit did not become a high-cost runtime setting.
- Created `QA ONLY - Assign Ember Email Bot - 2026-09-24` (`179fe0f2-3068-42e2-9c0a-501766a10766`) as a **Draft** with one exact Tag Added trigger and one `Update conversation AI bot and status` action. The builder's controlled Test Workflow run on `Ember Workflow QA` executed the assignment successfully at 3:35:14 AM CDT. This is direct assignment evidence, not a published broad trigger.
- Temporarily enabled Auto-Pilot on the QA bot only. A controlled reply to the prior incomplete-assessment email entered the same CRM conversation at 3:36 AM CDT. The QA bot's Agent Log identified the QA bot and generated an accurate answer: the free Growth Snapshot is available before booking, and the completed result appears on-screen with a browser-generated `.txt` download. The response reached AgentMail at 08:37:08 UTC in the same thread. The preset email greeting added a second `Hi` before that generated answer.
- Cleared the QA bot's preset email greeting and retested with a second internal question about whether the Snapshot is free. The AI answer reached the same inbox at 08:38:31 UTC with no duplicate salutation. The CRM conversation, QA bot Agent Log, and receiving inbox agree on the response. This proves internal inbound-email routing, bot generation, CRM send, and provider receipt for this one fixture; it does not prove production-bot rollout or general customer delivery.
- The published `002e - Reply Stops Pending Voice` trigger fired for both controlled email replies. Its logs show the fixture added at 3:36:58 and 3:38:21 AM CDT and `Mark Website Lead Engaged` executed; the contact gained `sales:engaged`. Some `Remove from workflow` sub-actions were still labelled `Processing` on inspection, so later-message suppression remains a timed check rather than a completed claim.
- After the second receipt, the QA bot was saved back to **Off**, and the one-off QA tag was removed from the contact. The QA assignment workflow remains Draft. No customer-facing production AI channel was enabled. The QA bot remains a reusable isolated test asset, not a live nurture channel.
- Published `004b - No-Show Recovery` and `004c - Cancellation Recovery` each had **0 total / 0 active** enrollments and both began by removing the contact-wide booked flag. Both were moved to **Draft** and saved. `004a - Appointment Showed & Recovery Cleanup` stays published. Until an exact-current-appointment guard and appointment-pair test exist, no-show/cancellation recovery requires a human to verify the triggering appointment and any newer confirmed booking before outreach; the unsafe automatic paths are held. [Appointment trigger](https://help.gohighlevel.com/support/solutions/articles/155000002619), [appointment If/Else filters](https://help.gohighlevel.com/support/solutions/articles/155000004050), and [goal events](https://help.gohighlevel.com/support/solutions/articles/155000003328) do not by themselves prove an exact appointment-ID comparison.

**Current release decision: controlled QA only, not traffic-ready.** Baseline/snapshot capture, CRM routing, two internal bookings, immediate booking confirmation, and the isolated AI email exchange have runtime proof. The next checks are the queued 9 AM booking-recovery delivery, the Sep 25 calendar reminders, consented SMS/voice, opt-out/handoff behavior, and a safe no-show/cancellation design. Production AI Email/SMS remains disabled pending Craig's customer-contact approval.

## 4:15 AM CDT opt-out signal and version-safety pass

- The published `005a - Website Opt Out` (`9329d2e2-bafe-48ae-83d4-078b01bd6378`) was exercised by adding its existing `stop bot` trigger tag to the internal `Ember Workflow QA` contact. Workflow logs showed the all-channel DND action executed at 3:49:52 AM CDT, followed by bot-stop and nurture-removal actions. A full contact reload showed All Channels, Email, Text and Calls DND enabled, plus `automation:pause`. This proves the **action** path for that fixture, not automatic detection of every natural-language opt-out.
- A separate `005c - Website DND & Unsubscribe Safety` (`3f2aa724-3985-4750-8d44-9533781d0490`) was cloned from 005a and published. Its copied DND write was **removed** to avoid a DND-trigger loop. It retains the website-source tag gate, pause marker, bot-stop and pending-nurture removal. The copied `Website Form Version = v1` condition initially sent a versionless internal fixture to `None`; it was removed, saved and retested. Re-entry and multiple-opportunity enrollment were both disabled. No outbound message action was added.
- 005c now has five explicit native signals: outbound DND enabled for **all channels**, outbound DND enabled for **Email**, **SMS**, or **Call** as a specific channel, and `Email events = Unsubscribed`. The Email/SMS/Call triggers each use the documented three-filter tuple: outbound direction, enabled for specific channels, and the exact channel. [HighLevel Contact DND](https://help.gohighlevel.com/support/solutions/articles/155000002673), [Email events](https://help.gohighlevel.com/support/solutions/articles/155000002678-workflow-trigger-email-events).
- All-channel DND on a name-only, website-tagged fixture enrolled 005c at 4:00:18 AM CDT after the tag-only gate fix. The workflow logged `Website Lead`, pause, bot-stop and removal, and the contact gained `automation:pause` and `stop bot`. A previous all-channel attempt hit `None` under the copied v1 gate; this was a detected and repaired test failure, not a pass.
- A name-only contact's Email DND checkbox persisted but did **not** enroll the earlier generic specific-channel trigger. That isolated variant remains unexplained. After using an owned AgentMail alias and adding the exact `DND channel = Email` filter, an Email-only DND event enrolled the contact at 4:05:18 AM CDT and completed the safety path. Separate synthetic `202-555-0172` and `202-555-0173` contacts passed the exact SMS and Call trigger paths at 4:10:36 and 4:12:06 AM CDT. No text or call was sent to those synthetic numbers. These tests establish the **specific configured tuples**, not general provider opt-out delivery.
- The original 005a also carried the unnecessary `Website Form Version = v1` gate. It was reduced to the website-source tag alone and saved. On the versionless internal Call fixture, a fresh `stop bot` tag event reached the Website Lead branch at 4:14:15 AM CDT, ran `Disable All Outbound` at 4:14:16, and a full CRM reload showed all-channel DND enabled. This protects future form versions and partial website contacts. One remove-from-workflow sub-action still displayed `Processing`; no later-message suppression claim is made until a timed check.
- `Email events = Unsubscribed` is configured but has **not** been exercised through a real unsubscribe link. The published production bot's Email/SMS channels remain undeployed, and no external-customer AI message was sent. 004b/004c remain Draft because exact-current-appointment protection is unresolved.
- Read-only inspection of 005b verified its trigger is the `human handover` tag, its eligibility branch requires only `source:phynyx-website`, and its Craig notification is an internal HighLevel **Notification** to Craig Capurso (not a customer message). A second internal-notification action names Andrew. No tag was fired overnight, to avoid an unnecessary staff alert; notification execution and receipt remain untested.
- Appointment-guard review found no documented native condition that proves an old no-show/cancellation event refers to the contact's **current** booking. An authenticated appointment resolver with a response-capturing Custom Webhook might supply that yes/no check, but that action is Premium and no resolver or pricing was verified; no paid feature was enabled. Until a safe appointment-pair test exists, 004b/004c stay Draft. The safe native fallback is an internal review alert only, with no contact-wide booked-tag removal, stage change, or customer message. [HighLevel appointment conditions](https://help.gohighlevel.com/support/solutions/articles/155000004050), [contact appointment API](https://marketplace.gohighlevel.com/docs/ghl/contacts/get-appointments-for-contact/), [Custom Webhook](https://help.gohighlevel.com/support/solutions/articles/155000003299).

**Current release decision remains controlled QA only.** DND/STOP signal-to-action paths now have bounded internal runtime evidence, while unsubscribe-link delivery, first clean-recovery email delivery, consented voice/SMS, human handoff, timed calendar reminders, safe no-show/cancellation recovery, and production AI channel isolation remain open. The 9:15 AM CDT thread follow-up remains active for business-hour checks.

## 4:40 AM CDT published Snapshot-journey regression pass

- AI Studio published the bounded funnel/copy correction; its publish panel read **Up to date**. The byte-identical published source is `backups/ai-studio/2026-09-24-published-snapshot-journey.zip`, SHA-256 `601ad6d8631a16137b32c96c3a376338d9f1386235e818e9c1c9af1338632d29`. Two independent checks of the exact export passed strict TypeScript, **76/76 tests**, and client+SSR builds. The source restores the single `Show My Growth Snapshot` completion POST, shows **Snapshot Saved** only for a 2xx-backed fingerprint matching current answers, excludes timestamp and CTA attribution from duplicate detection, and removes unverified personalized-review/questionnaire/email promises. The public assessment and schedule routes rendered after publication.
- One fresh, controlled, consent-off fixture, `Ember Final Snapshot QA` (`2oOj3tYs6VCFaU4GqDqk`, owned AgentMail alias and reserved `202-555-0174` test number), submitted the published five-field intake **once** at 4:35:03 AM CDT. CRM activity lists the exact AI Studio `get-my-free-growth-snapshot-intake` form event. Workflow **001** has one matching enrollment, finished; the contact gained the website source/form/version tags and a single New Website Lead opportunity.
- The same fixture submitted eight consistent estimated metrics through the published **Show My Growth Snapshot** click. The site displayed 8/8 tracked metrics, 5/5 valid rates, $25 cost/lead, $208 displayed cost/start, then **Snapshot Saved** without a second click. CRM activity lists one `growth-snapshot-assessment-completed` form event; workflow **001b** has one matching finished enrollment. The one opportunity moved to **Qualified / Needs Appointment**, while **002b** has one enrollment waiting 15 minutes, then the weekday 9 AM outbound window. No booking or message delivery is claimed for this fixture.
- CRM consent readback showed AI Voice Marketing, Marketing SMS, and Appointment Non-Marketing SMS Consent all **Not consented**; all three site checkboxes had remained unchecked. Reloading the saved assessment preserved **Snapshot Saved**. Re-entering the same tab with a different `cta` query also preserved Saved and showed no second submission control; CRM still showed one intake and one completion event. This is same-browser dedupe, not server-side idempotency.
- A pre-existing SSR hydration warning appears when reloading a saved in-progress step: server output starts at the free check, while first client render reads `sessionStorage`; the UI then recovers. It was present in the archived source and did not arise from this patch. Initial public SSR and both builds work, but a dedicated hydration-safe initialization remains a quality follow-up. The free-check CTA also says **Continue Without Numbers** after numeric inputs are entered; calculation and advance work, so this is a copy follow-up, not a capture failure.

**Release status remains controlled QA only.** This pass proves the published baseline-to-completion path and CRM routing for one new internal lead. It does not prove business-hour nurture delivery, deployed production AI replies, native timed reminders, human handoff, or safe no-show/cancellation automation. No paid feature or customer outreach was enabled.

## 4:45 AM CDT incomplete-path suppression check

- The fresh `Ember Final Snapshot QA` fixture entered `002a - Incomplete Assessment Recovery` when baseline intake completed at 4:35:05 AM CDT. Its exact workflow execution (`01M39C9GKM2YX8B2N9PFS813SH`) showed `Wait 15 Minutes` at 4:35:05, then **`Removed by - External workflow action`** and `Finished` at 4:35:55. No incomplete-recovery email action ran. This is direct evidence that the subsequent `001b` completion removed this fixture from the pending incomplete path before its first message; it does not prove all future timing races or provider delivery.
- In the same contact's exact `002b - Booking Recovery — Clean` execution (`01M39CB01CKB0NRMHBRFEJTZB0`), the 15-minute wait finished at 4:50:54 AM CDT. The unbooked/active conditions and concierge-assignment action executed by 4:50:59. The first email action then showed **Waiting**, with event details specifying Mon–Fri 9 AM–5 PM and `Next Execution At September 24th 2026, 9:00:00 am`. No email send or receipt is claimed yet. The event details showed `Using Contact's timezone (undefined)`; the scheduled local 9 AM must be checked against actual runtime delivery rather than assumed from that label.

## Model allocation decision

- Keep deterministic CRM state changes (capture, consent gates, waits, booking stop, opt-out and staff handoff) in workflows, not in an LLM. The high-reasoning pass is for architecture and exception review; repetitive live nurture replies are a narrower task. The concierge is currently configured for `OpenAI GPT-5.4 Mini`, which is the proportionate default while Email/SMS channels remain Off. Do not move that production bot to a higher-token model without a demonstrated failure on controlled booking, opt-out or handoff tests. Model choice does not substitute for channel isolation or provider-delivery proof.

## Internal appointment-status fallback

- `004d - Website Appointment Status Review (Internal)` (`43b3f613-50d8-4da1-a0b0-94607430d5ee`) was saved, **published**, and reopened. It merges exact `Phynyx Website - Assessment` no-show and cancellation status triggers, each gated to contacts tagged `source:phynyx-website`, into an internal task for Craig due the next weekday at 9 AM. The task directs staff to inspect the triggering appointment and any newer confirmed booking before any contact, opportunity or outreach change. It has no customer message/call, booked-tag or stage mutation, bot action, or paid webhook. The unsafe 004b/004c workflows remain Draft.
- On a **new owned QA contact**, appointment A (Sep 28) was cancelled after appointment B (Sep 29) was confirmed. `004d` logged its cancellation trigger, task action **Executed** at 5:00:06 AM CDT, and Finished; the contact's Tasks panel showed one Craig-assigned review task with the newer-booking warning. The contact retained `appt:booked` and one Appointment Scheduled opportunity because B was still confirmed: the stale-event safety case passed. B was then marked No Show. The exact no-show trigger enrolled at 5:02:38, task action **Executed** at 5:02:41, and Finished at 5:02:42; the Tasks panel showed a second Craig review task, one per distinct status event. No `004d` customer message executed.
- After B's real no-show, neither QA appointment was active, but the booked tag and Appointment Scheduled stage intentionally remained until a human reviews the task. This is the safety tradeoff: `004d` prevents a stale event from falsely reopening an already-booked lead, but it does not automate status cleanup or recovery after a genuine last-appointment no-show. During setup, the **native calendar**, separately from `004d`, sent two immediate confirmation emails to the owned test inbox even while that QA contact had all-channel DND. Do not infer that DND suppresses native transactional calendar email; verify its policy separately before traffic.
- A read-only booking-picker check offered B's Sep 29 8 AM slot again after it was marked No Show; no replacement booking was made. `004d` remained **Published** after reload, while 004b/004c remained **Draft with 0/0 enrollments**. The two QA tasks/contact remain as labeled evidence. The QA contact's now-stale booked tag/stage needs manual review; it is not evidence that automatic no-show recovery is complete.

## Direct-assignment channel isolation check

- A Draft **QA-only** workflow action targeted `QA ONLY - Ember Website Concierge - 2026-09-24` and set it Active for the owned, untagged `Ember DirectQA` contact. `Test Workflow` logged `Update conversation AI bot and status — Executed` at 4:53:36 AM CDT. At this checkpoint, assignment alone did **not** prove an AI email reply would bypass the channel's QA include tag. One internal setup email reached the owned AgentMail alias; an inbound reply probe was deferred because the QA bot detail page initially rendered blank. The later probe below resolved this uncertainty. The QA bot is back **Off** and its assignment workflow **Draft**.
- HighLevel's [current channel-routing documentation](https://help.gohighlevel.com/support/solutions/articles/155000008266-how-to-manage-conversation-ai-bot-channels-and-routing) says direct contact assignment, including the workflow assignment action, takes priority over normal channel/tag routing. Its [assignment-action documentation](https://help.gohighlevel.com/support/solutions/articles/155000003821-update-conversation-ai-bot-and-status-workflow-action) says the relevant channel must also be enabled. Therefore a tag-only Email include is **not a safe QA isolation gate** while production workflows directly assign the concierge. Production Email/SMS stays Off until assignment eligibility is narrowed or the direct assignment is removed, followed by controlled routing tests. The retained `owner:ember-ai` tag on human-booked contacts is still a semantic inconsistency; no live dependency audit proved removal safe, so that tag was not changed.

## 5:10 AM CDT additional delivery and guard checks

- The published `002b` first/day-2/day-7 eligibility branches were re-read in the live builder. Their website-source inclusion and booked, pause, stop-bot, handover and engaged exclusions are joined by **AND**, not a permissive OR. The direct `Update conversation AI bot and status` action still assigns `Ember - Website Sales Concierge` and sets Active on the qualified branch. No workflow edit was made.
- A later bounded QA-bot probe resolved the direct-assignment question experimentally. The `QA ONLY - Ember Website Concierge - 2026-09-24` bot's Email deployment required the `#qa-ember-email-20260924` include tag, but owned `Ember DirectQA` had **no such tag** and had been directly assigned that QA bot by the Draft workflow test. The QA bot was temporarily saved to Auto-Pilot; one inbound `ready` reply from the owned alias produced a HighLevel AI Bot response that was received back in AgentMail at 5:13 AM CDT. This proves direct assignment **bypassed the Email include-tag gate for this internal fixture**, consistent with HighLevel's routing documentation. The QA bot was then saved **Off**, reloaded, and its persisted Off mode reverified; the QA workflow remains Draft. The production concierge and its channels were untouched. Production Email/SMS must remain Off until any internal-only routing relies on a true assignment gate or direct assignments are removed, and the later production rollout receives explicit customer-contact approval.
- A real unsubscribe link in an already-delivered **002a** email to an owned QA alias opened HighLevel's **Unsubscribe successful** page. CRM activity recorded `Contact has unsubscribed from all emails` at 5:09 AM CDT. `005c` then enrolled via `Email Unsubscribed - Trigger` (`01M39E8GJETET91F0HXDJ594F3`) and finished; pause and bot-stop actions logged Executed. One remove-from-workflow action logged Executed and another still showed Processing after refresh. This **same** `Ember Workflow QA` contact already had all-channel DND and `automation:pause` from the earlier 005a STOP test at 3:49 AM, so its current DND/tags are **not** new evidence that unsubscribe alone created them. Later-message suppression remains a timed check.
- The owned appointment-status QA alias received exactly two native booking confirmations with `.ics` attachments, one for each test appointment, while all-channel DND was set. No email to that alias was observed after the cancellation/no-show events as of this readback. This is evidence for **these test messages**, not proof that cancellation/no-show notices are globally disabled. [HighLevel's calendar notification documentation](https://help.gohighlevel.com/support/solutions/articles/155000003441-calendar-email-in-app-appointment-notifications) says cancellation notices can cover canceled and no-show statuses and are configured separately by channel and recipient. A fresh live settings read was unavailable: the CRM API connection returned a scope error and the settings page stalled at Initializing. The existing prior calendar-settings audit remains the last readable configuration evidence.

## 5:20 AM CDT booked-contact AI handoff correction

- The published `003a - Appointment Booked & Team Notification` still had an `Ember - Optional Appointment Preparation` action that directly assigned the production concierge and set it **Active after booking**. This contradicted the human-led call handoff and would become risky if the production Email/SMS channels were later enabled, because direct assignment bypassed the QA tag gate in the controlled probe. The normal booking path already stops acquisition/rebooking workflows before staff notifications and opportunity updates; it did not independently make the bot inactive.
- The single action was renamed `Pause Ember after booking (human-led)` and changed to the same concierge with bot status **Inactive**. HighLevel exposed `Reactivate bot after` checked by default for Inactive; that checkbox was explicitly turned **off**. The action and published workflow were saved, then the page was reloaded: Published remained on, Saved was shown, and the reopened action showed Inactive with `Reactivate bot after` unchecked. Existing booking trigger, cleanup, Craig/Andrew internal notices, and opportunity branches were left unchanged. This is persisted **configuration** evidence, not a post-edit booking execution; the latter remains a staff-hours internal test.

## 5:35 AM CDT reply-to-human safety correction

- A live audit of published `002e - Reply Stops Pending Voice` found that a website lead's inbound reply stopped the pending nurture and added `sales:engaged`, but **created no human task or alert**. With the production bot's Email/SMS channels still undeployed, that could leave an interested reply waiting without an owner. Published `005b` is triggered only by the separate `human handover` tag; 002e did not add that tag.
- After the existing stop and engaged actions, 002e now creates `Website lead replied — review and respond`, assigned to Craig, due the next weekday at 9 AM, and sends Craig an immediate **in-app** notification opening the conversation. The task directs Craig to inspect the latest message, respond if Ember has not, help the lead book, honor opt-out, and not restart nurture automatically. No customer message, call, or AI channel was enabled. The notification and task are complementary: immediate visibility plus a durable work item.
- The 002e trigger's extra `Website Form Version = v1` filter was removed while preserving `Customer replied` plus `Has tag = source:phynyx-website`. This matches the earlier versionless-contact safety correction in 005a/005c and avoids missing a real website reply after a form-version change or partial field write. Each edit was saved to the published workflow; a full reload showed **Saved**, **Published**, the source-only trigger, and both new actions. This is configuration readback. The **post-edit task, alert, and suppression execution** still require a controlled owned-contact reply test during staff hours.

## 6:00 AM CDT direct-booking lead handoff repair

- A new, owned, consent-off visitor submitted only the four required `/schedule` contact fields and stopped at slot selection. CRM created the contact and applied `automation:phynyx-web-v1`, `source:phynyx-website`, and `sales:booking-followup`, but its record showed **No Opportunities Yet**. No booking was made. This exposed a direct-only pipeline visibility gap: 001 created an opportunity for Growth Snapshot intake, while 001b did not create one for direct scheduling.
- Published 001b now ends with **Create opportunity** in `Phynyx — Website Sales` at `New Website Lead`, with duplicate opportunities **Disabled**. The existing two exact AI Studio form triggers and four prior actions were preserved. For completed Snapshot leads already carrying an opportunity in that pipeline, the duplicate-disabled setting is intended to avoid a second one; that cross-path dedupe still needs a fresh runtime test.
- A second, distinct owned, consent-off direct `/schedule` submission reached slot selection and stopped without booking. The CRM record shows the source and booking-followup tags plus **one open Phynyx — Website Sales / New Website Lead opportunity** and an activity entry that the opportunity was created. This proves direct-only lead capture to pipeline for that controlled fixture, not message delivery, appointment creation, or a human owner assignment.

## 6:15 AM CDT terminal nurture handoff

- A bounded audit found that 002b's day-7 eligible branch ended after `Day 7 — Close the Loop`; it documented no human task for a still-unbooked, silent lead. The reply-specific 002e task does not cover nonresponders. Therefore the previous finite-cadence configuration alone did not justify a “no lead dropped” claim.
- Published 002b now inserts **Day 7 — Human Review Queue** on the existing `Unbooked and active` day-7 branch before the final email. It creates a Craig-assigned task, due one day later at **9:00 AM** with weekends skipped. The task instructs Craig to verify whether the final email actually sent, review the latest conversation/opportunity, close it if the lead booked/replied/handed over/opted out, and check consent/DND before personal outreach. The final email remains connected after this action. Reload readback confirmed **Saved**, **Published**, the task's assignee/deadline/weekend setting, wording, and the final email. This is configuration evidence; the seven-day runtime path and task creation are not yet proven.
- The existing 9:15 AM CDT task heartbeat was extended without daily runs to four checkpoints: September 24–25 for business-hour delivery/booking/reminder tests and October 1–2 for the separate, deliberately unbooked direct-intake fixture's day-7 task, final-email delivery, and task due time. It remains active and is instructed to stay quiet on unchanged waits and make no workflow/bot/domain/billing changes.

## 6:30 AM CDT published booking-safety and fallback revision

- The final AI Studio source keeps the same capture forms, consent defaults, native submission events, calendar, CRM workflows and domain. Its bounded booking changes distinguish pre-POST availability failures from ambiguous post-POST outcomes, require an explicit appointment ID and an exactly matching zoned start before the UI claims confirmation, and treat a 2xx without verifiable fields as **accepted but unverified** with no immediate resubmit prompt. An in-flight guard prevents repeated Confirm clicks. A malformed availability response—including a non-string, empty or timezone-less slot—is a retryable **read** error that preserves the visitor's selected time and makes **no booking POST**. A valid empty availability result presents the existing `/support` contact path after lead capture. The free-check CTA now reads **Continue to My Snapshot** whether or not three numbers were entered.
- A separate read-only code audit found the non-string-slot validation edge after an earlier 109-test pass; AI Studio repaired it before publication. The final exported ZIP independently passed strict TypeScript, **122/122 tests in seven files**, and both client and SSR production builds. The tests cover 2xx malformed booking bodies, malformed availability, no-times fallback, preserved selection and zero POST on preflight errors. These are source/component proofs, not live provider idempotency or booking delivery proofs.
- AI Studio's publication dialog changed from **Publishing...** to **Up to date** for `https://phynyxpro-website.vibepreview.app`. The post-publication export was byte-identical to the tested pre-publication ZIP: SHA-256 `6203599c4a228cdc97e0ce1dfb7310f0dfc0c63a4d3ce2b8262b51fd6062b194`. It is archived as `backups/ai-studio/2026-09-24-published-booking-safety-and-fallback.zip`. The public `/growth-assessment` page rendered the corrected CTA both empty and with 80/28/20 inputs, showing 35%, 71.4%, and 28.6%; public `/schedule` rendered the four required contact fields and unchecked optional appointment-SMS consent. No public-page console errors were observed on `/schedule`. No new form submit or booking was made in this publication check.

**Current release decision: controlled QA only, not traffic-ready.** Direct-only and Snapshot lead capture reach HighLevel, and a direct-only fixture now has a New Website Lead opportunity. The final site source/build and rendered public pages pass. Still unproved are the clean-recovery email's provider delivery, consented SMS/voice, the post-edit booked-bot pause and reply-to-human task/alert, future reminders, day-7 task/final email, cross-path opportunity dedupe, and production AI channel isolation. The production concierge's Email/SMS channels remain Off; no custom domain is connected; unsafe automatic 004b/004c no-show/cancel flows remain Draft. The scheduled 9:15 AM CDT business-hour checks and later day-7 checkpoints are the next bounded verification, not an approval to send customer-facing AI outreach.

## 6:50 AM CDT completed-Snapshot stage correction and real-form proof

- A controlled new baseline-then-completion submission initially exposed a narrow stage gap: 001 created one `New Website Lead` opportunity, and 001b correctly skipped its duplicate-disabled Create opportunity action, set `Snapshot Status = Completed`, removed the incomplete marker and entered booking follow-up, but the existing opportunity **stayed New Website Lead**. Completion is not booking, yet this lead should be marked `Qualified / Needs Appointment` to distinguish the finished diagnostic from a baseline-only or direct-schedule visitor.
- Published 001b now has a single `Snapshot Status is Completed` branch after its existing recovery cleanup. That branch runs **Find opportunity** for the triggering contact's most recently created `Open` opportunity in `Phynyx — Website Sales`, then **Update opportunity** on the Found branch to `Qualified / Needs Appointment`. Its Not Found branch ends without changing another record. The native Update action needs the preceding Find action for a form-triggered workflow, as documented by [HighLevel Find Opportunity](https://help.gohighlevel.com/support/solutions/articles/155000004751-workflow-action-find-opportunity) and [Update Opportunity](https://help.gohighlevel.com/support/solutions/articles/155000004753). Backward stage movement remains disabled so an already-booked stage is not intentionally downgraded by a repeat Snapshot. The direct-only/blank-status branch ends at its existing `New Website Lead` state. The workflow was saved with Publish on and showed `Saved` on readback.
- The existing owned, consent-off Crosspath fixture was run through the saved workflow test. CRM changed its same opportunity from `New Website Lead` to `Qualified / Needs Appointment` while `Opportunities (1)` remained unchanged; this is an internal workflow-test result, not a new site event.
- More importantly, a **new published-site** owned fixture went through `/growth-assessment`: the ungated free check, five-field baseline capture with all three optional SMS/AI-call consents unchecked, business context, and Snapshot save. Three of eight funnel metrics were estimated; the result honestly showed 3/8 tracked and 0/5 calculable adjacent rates, with no invented forecast. `Snapshot Saved` appeared without a second submit. CRM created the contact with `Snapshot Status = Completed`, `sales:booking-followup`, and exactly **one** open Website Sales opportunity at `Qualified / Needs Appointment`; no appointment was made.
- The exact 001b enrollment for that real completion form fired at 6:46:59 AM CDT and finished on `Move completed snapshot opportunity to Qualified`. Its execution rows show `Create opportunity — skipped` at 6:47:07, `Snapshot completed — Executed` at 6:47:13, `Find opportunity — Executed` at 6:47:15, and `Move completed snapshot opportunity to Qualified — Executed` at 6:47:18. This proves cross-path dedupe, the field gate, lookup, and forward stage update together for one new internal lead. It does not prove all repeat-submission races or later nurture delivery.
- A small nonblocking UX issue remains: numbers entered in the free 3-number check were not prefilled into the corresponding fields of the longer Snapshot, so visitors may re-enter them. The longer form permits `Not tracked`, and the result correctly declines to calculate rates when adjacent stages are missing. This is a friction/expectation follow-up, not a failed capture.

**Latest release decision: controlled QA only, not traffic-ready.** The published real-form baseline-to-completion path now reaches one CRM lead, one opportunity, and the correct pre-booking stage. Still open: first 002b email/provider delivery at business hours, affirmative-consent voice/SMS tests, production AI channel isolation and customer-outreach approval, post-edit 002e reply-to-human and 003a booked-bot executions, 24h/1h reminders, day-7 task/final email, safe automatic no-show/cancellation recovery, and a custom domain. No paid feature or customer-facing AI channel was enabled in this pass.

## 7:15 AM CDT public calendar regression repair and booking check

- A fresh internal `/schedule` test revealed **“Couldn't load times”** before a booking could be attempted. The native HighLevel assessment widget independently listed September 25 appointment slots. A bounded source audit traced the mismatch to the booking-safety publication: the live LeadConnector free-slots response was HTTP 200 with valid date/slot arrays **and** a top-level `traceId` string, while the new site validator required every top-level value to be a date bucket. Thus a valid provider response was rejected. This was a pre-booking read failure, not a calendar with zero availability.
- AI Studio stripped only the known top-level `traceId` metadata before the existing strict date, ISO-time, and slot validation; three focused tests cover this provider envelope. No form, consent, booking POST, workflow, or calendar logic changed. A first AI Studio draft passed 125 tests but failed independent TypeScript on a dot-property access; the one-line bracket-notation correction was re-exported and independently passed strict TypeScript, **125/125 tests**, and client/SSR builds. Its full source differs from the prior published ZIP only in `src/lib/calendar.ts` and `tests/calendar-booking.test.ts`.
- Preview and then freshly loaded **public** `/schedule` displayed selectable September 25 slots from 8:00 AM through 4:30 PM CDT. AI Studio showed **Up to date** after publication. The final source is archived as `backups/ai-studio/2026-09-24-published-calendar-availability-fix.zip`, SHA-256 `08d150a1822dab97421e6e5830b7533704a448a755124250197ba8916287a8ca`.
- One owned internal public-site booking selected **September 25 at 9:30 AM CDT**. The site displayed the expected **accepted but unverified** result, and no retry was made. Fresh CRM readback found an exact **Confirmed** assessment appointment assigned to **Dr. Andrew Higdon**, an `appt:booked` tag, and an opportunity advanced from **New Website Lead** to **Appointment Scheduled**. AgentMail metadata independently showed one matching native confirmation with an `.ics` attachment. This proves appointment creation and confirmation delivery for this fixture; the site still did not receive enough response fields to assert confirmation itself.
- The test reused a synthetic phone already associated with an earlier QA contact. HighLevel matched that existing contact and replaced its primary test name/email, so this is **not** a clean new-contact or nurture-suppression test. A CRM note records both former and current QA identities, the prior unsubscribe/DND/pause state, and the internal booking. No real customer record was affected. Future fixture phones must be checked for uniqueness before submission.
- The exact published `003a` run (`01M39NBHH5MTS0107CGFPW3QNN`) enrolled at 7:13:28 AM CDT and finished at 7:13:40. Its successful removal event names `002b - Booking Recovery — Clean` and `002c - Consented Voice Outreach`; Craig and Andrew internal-notification actions, Find opportunity, and Update opportunity each executed. This is workflow execution proof, not staff-notification inbox delivery. The configured `Pause Ember after booking (human-led)` action is saved as **Inactive, no reactivation**, but this run **did not execute it**: the existing `automation:pause` tag made its branch false. A separate clean booking is still needed to prove that branch. Andrew's assignment is observed on the appointment and contact but cannot be credited to 003a, which has no owner-assignment action.

**Release decision remains controlled QA only.** Public calendar availability and a confirmed internal booking now work. The timed 002b recovery email, active-nurture removal on reply, post-edit 003a bot pause, consented SMS/voice, future reminders, day-7 task, and safe AI channel isolation still need runtime checks. Production concierge Email/SMS remain Off; no customer-facing AI outreach or custom-domain cutover was authorized by this proof.

## 7:25 AM CDT reply-to-human runtime proof

- An owned internal BookingFix alias replied to its existing native booking confirmation with a plainly labeled QA question. CRM ingested that exact inbound email at 7:19 AM CDT. Published `002e - Reply Stops Pending Voice` enrolled at 7:19:48 and finished at 7:19:54. Its `sales:engaged` action executed and the tag is present on the contact.
- The newly added **Website lead replied — review and respond** task executed at 7:19:52 and is visible as an active Craig-assigned task due September 25 at 9:00 AM CDT. The in-app notification executed at 7:19:53 and is visible in Craig's bell drawer with the expected conversation prompt. This is runtime and in-app readback, not evidence of an email/SMS notification.
- The workflow's removal action targets 002a, both 002b versions, 002c and 002d. Its child logged Success, but details said this booked contact was **not enrolled** in any selected workflow at that moment; one parent row still displayed Processing. Therefore this fixture proves reply ingestion, engaged tagging, task creation and notification, **not removal of an active nurture run**. A fresh unbooked fixture is still needed for that suppression proof.
- A time-boxed live tag-dependency inspection found no `owner:ember-ai` condition in inspected 001, 002b Clean, 003a, 004a or 005b paths; those visible gates instead use source, booking, pause, opt-out, handover and engagement. The other website workflows and any reporting consumers were not exhaustively checked. The tag is stale-looking on a human-owned booked record, but removing it is nonessential to lead capture and could alter reporting. It was therefore left unchanged pending a small scoped dependency/meaning decision; it is **not** being used as proof that Ember owns the booked conversation.
- The existing 9:15 AM CDT verification heartbeat remains Active for September 24–25 and October 1–2. Its prompt was narrowed to avoid repeating the proven booked-contact reply, check one unbooked active-nurture removal, verify synthetic-phone uniqueness before another booking, and, **only after 002b email delivery is proven**, remove 002b's broad direct concierge-assignment action while preserving/publishing the guarded email path. Production concierge channels must remain Off; any old direct assignments require separate inventory before a QA-only channel test. This is a scheduled scoped next step, **not** a change already made to 002b or proof of future delivery.
